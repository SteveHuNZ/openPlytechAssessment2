<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bit603_a2_stevehu">

    <application
        android:name=".CustomApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BIT603_A2_SteveHu"
        tools:targetApi="31">
        <activity
            android:name=".ItemListActivity"
            android:label="@string/menu_inventory_status_btn"
            android:parentActivityName=".MenuActivity"
            android:exported="false" />
        <activity
            android:name=".AddItemActivity"
            android:label="@string/menu_add_items_to_inventory_btn"
            android:parentActivityName=".MenuActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/login_btn"
            android:exported="false" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/inventory_management"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>